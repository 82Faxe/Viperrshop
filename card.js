function rotateCard(card) {
    card.style.transform = "rotateY(10deg) rotateX(10deg)"; // Поворот карточки
}

function resetCard(card) {
    card.style.transform = "rotateY(0deg) rotateX(0deg)"; // Сброс поворота
}